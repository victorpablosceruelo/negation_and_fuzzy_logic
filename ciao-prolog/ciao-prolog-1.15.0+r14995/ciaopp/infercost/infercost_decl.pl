:- package(infercost_decl).
:- use_package(nativeprops).

% Uncomment the next lines if you like to use rtcheck to debug the
% cost analysis -- EMM

% :- use_package(rtchecks).

% :- rtchecks_opts([coding(inline), callloc(predicate)]).
