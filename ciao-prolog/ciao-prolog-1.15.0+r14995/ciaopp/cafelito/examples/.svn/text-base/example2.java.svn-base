
public class example2{

    // Sum from 1 to N
    public static int sum1toN(int n){
	int sum = 0;
        while (n>=1){
	    sum=sum+n;
	    n=n-1;
	}
	return sum;
    }

    //Greatest common divisor
    public static int gcd(int m, int n) {

        if (m < n) {
            int t = m;
            m = n;
            n = t;
        }

        int r = m % n;

        if (r == 0) {
            return n;
        } else {
            return gcd(n, r);
        }
    }

    //recursive factorial
    public static int recfact(int n) {
	if ( n == 1)
	    return 1;
	else
	    return n * recfact(n-1);
    }

    //iterative version still to be done (for!)
}





