:- load_resource_module(res_gounif(res_gounif_res)).

:- head_cost(ub, gounif, f_gounif).
:- head_cost(lb, gounif, f_gounif).

:- literal_cost(ub, gounif, f_gounif).
:- literal_cost(lb, gounif, f_gounif).

:- trust_default + cost(ub, gounif, 0).
:- trust_default + cost(lb, gounif, 0).
