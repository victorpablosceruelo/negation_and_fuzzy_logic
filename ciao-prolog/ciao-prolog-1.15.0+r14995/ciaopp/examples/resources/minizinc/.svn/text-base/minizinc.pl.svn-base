:- package(minizinc).

:- use_package(ciaopp(tests(resources))).

:- use_package(assertions).
:- use_package(regtypes).
:- use_package(nativeprops).
:- use_package(isomodes).
% :- use_package(resprof).
:- use_package(ciaopp(examples(resources(minizinc(constraints_res))))).
:- use_package(ciaopp(examples(resources(minizinc(minizinc_assrts))))).

% should work, but... don't
%:- load_compilation_module('../ciao-lib/clpfd_lib').

% for analysis (but don't hurt otherwise)

:- use_module(ciaopp(examples(resources(minizinc(minizinc_rt))))).

% :- redefining(is/2).
