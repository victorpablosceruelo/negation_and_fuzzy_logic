
:- module(database,
	[ approximation/1,
          db_get/1,
          flag_is_not_fails/1,
          flag_is_possibly_fails/1,
          flag_is_covered/1,
%          flag_is_not_covered/1,
%          flag_set_not_fails/1,
          flag_set_possibly_fails/1,
%          flag_set_covered/1,
          flag_set_not_covered/1
	],
	[]).

approximation(_).
db_get(_).
flag_is_not_fails(_).
flag_is_possibly_fails(_).
flag_is_covered(_).
% flag_is_not_covered(_).
% flag_set_not_fails(_).
flag_set_possibly_fails(_).
% flag_set_covered(_).
flag_set_not_covered(_).

%-------------------------------------------------------------------------
