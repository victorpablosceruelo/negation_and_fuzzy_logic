
:- module(color,
	[ binary_disequality/4,
	  gcp/4
	],
	[]).

:- use_module('..'(init)).
:- use_module('..'(size)).
:- use_module('..'(top)).

:- use_module(library(lists), [length/2	]).

:- push_prolog_flag(multi_arity_warnings,off).

:- include(disequality).
:- include(gcp).
:- include(cslpoly).

:- pop_prolog_flag(multi_arity_warnings).
