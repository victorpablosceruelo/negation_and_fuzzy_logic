To check this benchmark, type the following in a Ciaopp buffer (with
default options): 

ciaopp ?- set_pp_flag(local_control,off).
ciaopp ?- set_pp_flag(fixpoint,di).
ciaopp ?- set_pp_flag(collapse_ai_vers,off).
ciaopp ?- use_module(plai(intermod)).
ciaopp ?- auto_analyze(shfr,go).
...
ciaopp ?- auto_transform(shfr,spec,go).
...

*_out.pl files are generated with the results of specializing the
modular program.


