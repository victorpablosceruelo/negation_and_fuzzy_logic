:- module(test_fuzzy, [main/0], [assertions]).

:- use_module(library(format)).
:- use_module(test, [test/0]).

% main:-
%         format("**** Fuzzy Prolog cannot be tested at the moment~n", []),
%         format("**** due to the problems of having two constraint~n", []),
%         format("**** systems at the same time~n", []),!.

:- test main.

main:-
	nl,
	test.
