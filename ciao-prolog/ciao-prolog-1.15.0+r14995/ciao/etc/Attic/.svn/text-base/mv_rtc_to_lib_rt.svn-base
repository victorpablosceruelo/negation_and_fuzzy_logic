#!/bin/sh

DIRNAME=`dirname $2`
TARGET_DIRNAME=$1/${DIRNAME:2}
echo "/bin/mkdir -p $TARGET_DIRNAME && /bin/cp $2 $TARGET_DIRNAME"
/bin/mkdir -p $TARGET_DIRNAME && /bin/cp $2 $TARGET_DIRNAME && /bin/rm -f $2
