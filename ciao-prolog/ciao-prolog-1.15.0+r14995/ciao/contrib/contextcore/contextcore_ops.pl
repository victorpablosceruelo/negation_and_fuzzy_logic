% JFMC: it different than for fsyntax (I believe we should change that in fsyntax too, but it conflicts with | in [A|As])
% :- op(600, xfy, ('|')).

:- op(  25,  fy, (^)).
:- op( 700,  xfx, (<-)).
:- op(50, yf, [(@)]). % todo: sure?


