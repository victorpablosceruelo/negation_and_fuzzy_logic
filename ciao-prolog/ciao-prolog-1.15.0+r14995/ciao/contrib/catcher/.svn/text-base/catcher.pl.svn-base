:- package(catcher).
% A Catcher package for catch exceptions.

:- load_compilation_module(library(catcher(catcher_tr))).

:- use_module(library(catcher(catcher_rt))).

:- add_sentence_trans(catcher_sentence_tr/3, 750).

% TODO: Fix those bugs
% - This package should use goal expansions (not sentence tranlations)
% - Missing documentation
% :- doc(bug,"The catcher is not compatible with interrupt/3").
