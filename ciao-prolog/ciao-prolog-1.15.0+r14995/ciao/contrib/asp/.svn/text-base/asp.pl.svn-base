:- package(asp).
:- load_compilation_module(library(asp(h3))).
:- add_sentence_trans(sn/3, 750). % TODO: right priority?
:- add_term_trans(tm/3, 750). % TODO: right priority?
%:- add_clause_trans(cm/3, 750). % TODO: right priority?

%:- op(990, fy, not).
