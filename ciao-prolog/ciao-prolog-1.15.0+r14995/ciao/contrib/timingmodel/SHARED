# -*- mode: Makefile; -*-

SRC=$(CIAODESRC)
SRCDIR=$(CURDIR)

ifndef MPARCH
MPARCH:=$(shell $(CC) -dumpmachine)
endif

# optim-comp:
# PROLOG=$(shell eval `$(CIAODESRC)/optim_comp/ciaotool bash-env` && echo $$CIAOCACHE)/bin/ciaosh

# miniprolog:
PROLOG=    $(shell cd $(SRCDIR)/miniprolog && $(MAKE) -s --no-print-directory get_prolog)
PROFREPORT=$(shell cd $(SRCDIR)/miniprolog && $(MAKE) -s --no-print-directory get_profreport)
