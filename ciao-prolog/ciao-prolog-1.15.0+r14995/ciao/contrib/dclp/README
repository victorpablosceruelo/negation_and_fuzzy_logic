"Distributed Constrant Logic Programming"

  This package is unstable and undocumented.

BUGS:
  
  % Wrong behaviour
  ?- use_package(library(dclp(and))).
  ?- all_different([A,B,C]), A .>. B, B .>. C, A = 3, C = 1.
  no

  % Right behaviour
  ?- use_package(library(clpfd)).
  ?- all_different([A,B,C]), A #> B, B #> C, A = 3, C = 1.
  A = 3,
  B = 2,
  C = 1 ? 


