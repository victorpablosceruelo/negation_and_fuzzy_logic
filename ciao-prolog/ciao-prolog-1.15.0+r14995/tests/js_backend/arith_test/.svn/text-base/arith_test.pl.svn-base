:- module(arith_test, [], [oo_syntax, hiord, assertions, fsyntax, mutables, string_type]).

:- doc(title, "Test for arithmetic").

:- use_module(engine(io_basic)).

:- export(main/0).
main :-
	arith_test.

arith_test :-
        M is 10 + 100,
	display(M), nl.

