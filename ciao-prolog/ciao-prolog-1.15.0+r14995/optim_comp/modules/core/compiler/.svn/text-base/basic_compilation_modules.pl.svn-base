:- module(_, [], []).

% This module loads compilation modules required to compile the
% compiler. They will be included statically in the bootstrap.

:- use_module(library(metatypes(expansion))).
:- use_module(library(regtypes(regtypes_tr))).
:- use_module(library(runtime_ops_tr)).
:- use_module(library(foreign_interface(foreign_interface__syntax))).
