:- package(complang).

% This package setups the language extensions and packages that
% defines the language in which the compiler is written
% (i.e. assertions, high order, functional notation, instantiable
% modules, contexts, etc.).
% 
% Author: Jose F. Morales

:- use_package(assertions).
:- use_package(basicmodes).
:- use_package(isomodes).
:- use_package(regtypes).
:- use_package(hiord).

:- use_package(compiler(complang_mini)).

:- use_package(compiler(compiler_object)).

