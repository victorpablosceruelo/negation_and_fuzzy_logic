:- package(js_lang).

% :- doc(title, "Predicates defined as JS code by the user.").
%
% The basic definitions are:
%
%  js_lang.stats(Stats)    # "Statements"
%  js_lang.stats(Stats, R) # "Statements returning @var{R}"
%  js_lang.expr(Expr, R)   # "Expression returning @var{R}"
%    (shorthand for js_lang.stats([return(Expr)], R)
%
% The extended definition allow further interaction with Prolog
% control:
%
%  js_lang.test(Expr)      # "Boolean expression (semidet)"
 
:- '$pragma'(allow_js_lang).

